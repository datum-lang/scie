pub struct JavaScriptFacet {
    pub is_frontend: bool,
    pub is_angular: bool,
    pub is_react: bool,
    pub is_vue: bool,

    pub is_node: bool,
    pub is_browser: bool,
    pub is_ionic: bool,
    pub is_cordova: bool,
    pub is_bower: bool,
    pub is_hybrid: bool,

    pub is_typescript: bool,
    pub is_javascript: bool,

    pub has_grunt: bool,
    pub has_gulp: bool,
}